PORT=3000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key_academic_submission_2026

# Optional Production Persistence Paths (PaaS Render/Railway Disk Volume)
# DATABASE_PATH=/data/database.db
# UPLOAD_DIR=/data/uploads

# Email Configuration (Nodemailer Abstraction)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
FROM_EMAIL=no-reply@society-maintenance.com
